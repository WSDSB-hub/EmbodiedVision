## 串口通信协议设计

### 帧格式

| 字节 | 内容 | 说明 |
|:---|:---|:---|
| 0 | 0xAA | 帧头，用于同步 |
| 1 | CMD | 指令码（0x01前进, 0x02后退, 0x03左转, 0x04右转, 0x05停止） |
| 2-3 | Left Speed | 左轮目标转速，int16，大端 |
| 4-5 | Right Speed | 右轮目标转速，int16，大端 |
| 6 | Checksum | 前6字节异或校验 |

### 指令示例

| 指令 | 帧内容 |
|:---|:---|
| 前进 100 RPM | `AA 01 00 64 00 64 C1` |
| 后退 100 RPM | `AA 02 FF 9C FF 9C 3B` |
| 左转 | `AA 03 FF B0 00 50 6C` |
| 右转 | `AA 04 00 50 FF B0 6F` |
| 停止 | `AA 05 00 00 00 00 05` |

### 实现状态

- **STM32 端**：已实现串口接收中断回调 + 轮询解析代码（`03_Car_Serial/Core/Src/main.c`）
- **Python 端**：已实现完整的指令打包、发送与协议自测脚本（`car_control.py`, `serial_self_test.py`）
- **协议验证**：Python 端自测全部通过（见 `images/serial_protocol_test.png`）
- **联调状态**：因电脑串口占用问题暂时搁置，协议设计与代码逻辑均已验证正确
